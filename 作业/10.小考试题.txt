冒泡排序
int[] arr1 = new int[]{98,78,65,10,54};
实现从大到小排序 降序


写出String类型对应方法或者方法对应含义

写出对应方法：
例如：返回字符串按照'-'分割    只需要回答 .split('-')
1.返回字符串长度：
2.返回字符串第0位置字符：
3.返回字符串中第一个字符i的位置：
4.返回字符串中最后一个字符i的位置：
5.将字符串转为byte类型数组：
6.将字符串中大写字母改为小写字母

写出方法对应含义并指明参数含义：
1. .trim()
2. .substring(int a,int b)
3. .valueOf() （此处写出valueOf括号中可以传什么类型的参数）
4. .endsWith(String str)
5. .compareTo(String str)


