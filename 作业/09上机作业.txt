1.从控制台输入2次密码，判断是否一致
2.输入一个车牌号将将牌号中字母转成大写形式
3.输入身份证号，判断长度是否合法。
4.输入5个姓名，按照字符长度的升序进行排列并输出
5.控制台输入Email地址，粗略判断其是否为合法Email(@是否存在)
6.控制台输入一组由字母组成的字符，查找所有a的位置
7.控制台输入一组由字母组成的字符,找出26个字母出现的频次
8.控制台输入Email地址，输出账户名和服务名
9.控制台输入文件名，判断是否为图片(html可用：jpg、png、gif)，并将文件的逻辑名替换成一个随机数字
10.假定一个类的字节码文件uri(路径+文件名)为：“/com/enity/User.class”，输出这个类的完全限定名
11.请求参数的内容为：id=lisi&pwd=123456,其中“=”左侧为请求参数名，右侧为请求参数值，多个请求参数用“&”间隔，分割字符串得到每一个参数名和参数值，并输出

12.控制台输入一个ip地址，分别输出4个ip段的值

0|123|4
h|ell|o